const config = {
    recast: {
        token: 'Recast request token',
        language: 'en',
    },
    smooch: {
      keyId: 'Smooch keyId',
      secret: 'Smooch secret',
    },
    port: 8080,
}

module.exports = config
